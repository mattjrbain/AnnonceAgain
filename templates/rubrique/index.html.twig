{% extends 'base.html.twig' %}

{% block title %}Hello RubriqueController!{% endblock %}


{% block body %}
    <div class="row">
        {% for rubrique in rubriques %}
            <div class="col-lg-3 col-md-4 mt-3 ">
            <div class="card text-center shadow h-100 cardHome bg-lightblue" style="min-width: 225px">
                <div class="card-body d-flex flex-column">
                    <h1 class="card-text text-break text-capitalize "><a href="{{ path('annonce_rubrique', {'id': rubrique.id}) }}" class="card-link   text-dark stretched-link">{{ rubrique.libelle }}</a></h1>
                </div>
            </div>
            </div>

        {% endfor %}
    </div>
{% endblock %}
